<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="shortcut icon" href="/logo.ico" type="image/x-icon" />
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <link href='https://fonts.googleapis.com/css?family=Mulish' rel='stylesheet'>
    <!-- Font Awesome JS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <!-- Upload CSS-->
    <link href="https://cdn.jsdelivr.net/gh/kartik-v/bootstrap-fileinput@5.2.5/css/fileinput.min.css" media="all" rel="stylesheet" type="text/css" />
    <style>
        body {
            background-image: url(/background.png);
            background-position: center;
            background-repeat: none;
            background-attachment: fixed;
            background-size: cover;
        }

        .container {
            width: 65%;
            height: 75%;
            background-color: white;
            border-radius: 20px;
            box-shadow: 4px 4px #c8c8c6;
            /* Center vertically and horizontally */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .container-child {
            width: 100%;
            position: relative;
        }

        .logo {
            margin: 0;
            padding: 0;
            width: 45%;
        }

        .img-logo{
            width: 80%;
            height: 80%;
            object-fit: contain;
        }

        .content {
            width: 55%;            
        }

        .content .text {
            font-size: 2rem;
            font-weight: bold;
            padding: 3.5rem 0 2rem;
        }

        .content .form{
            width: 75%;
            margin-bottom: 0.5rem;
        }

        .form-group{
            width: 100%;
            height: 4.5rem;
            margin-bottom: 1.5rem;
            padding: 0;
            position: relative;
        }

        .form-group img {
            position: absolute;
            top: 1rem;
            left: 0.25rem;
        }

        .image {
            width: 2rem;
            padding: 0.5rem;
            min-width: 2.5rem;
        }

        .input-field {
            width: 100%;
            height: 100%;
            padding: 0 4rem;
            border-radius: 50px;
            border: 2px solid #BDE6F1;
            outline: none;
        }

        .input-field:hover{
            border-color: #C6F0FA;
        }

        .input-field:focus{
            border: 3px solid #BDE6F1;
        }

        .form-group .label{
            position: absolute;
            left: 3rem;
            top: 1.5rem;
            font-size: 1rem;
            padding: 0 0.5rem;
            background-color: white;
            opacity: 0.8;
            transition: 
                top 200ms ease-in,
                left 200ms ease-in,
                font-size 200ms ease-in;
        }

        .submit{
            display: block;
            padding: 0.3rem 1.5rem;
            background-color: #92D2C3;
            color: #FEFEFE;
            border-radius: 1rem;
            border: none;
            outline: none;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 200ms ease-in;
        }

        .submit:hover{
            background-color: #73ccb7;
        }

        .input-field:focus + .label{
            top: -1rem; 
            font-size: 1.1rem;
            opacity: 1;
            color: #67cae3;
        }

        .input-field:not(:placeholder-shown).input-field:not(:focus) + .label {
            top: -1rem; 
            font-size: 1.1rem;
            opacity: 1;
            color: #67cae3;
        }

        .sub-text{
            font-size: 1.2rem;
            font-weight: 500;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .checkPass1, .checkPass2{
            position: absolute;
            z-index: 10;
            width: 100%;
        }

        #togglePassword1, #togglePassword2{
            position: absolute;
            top: 1.7rem;
            right: 1rem;
            font-size: 1rem;
            cursor: pointer;
        }

        .danger_icon{
            padding-right: 10px;
        }
        
    </style>
    <title>Quên mật khẩu</title>
</head>
<body>
    <div class="container d-flex flex-row align-content-center">
        <div class="container-child d-flex flex-row ">
            <div class="logo d-flex justify-content-center align-content-center">
                <img src="/logo.png" alt="" class="img-logo align-self-center">
            </div>
            <div class="content d-flex flex-column align-content-center">
                <h2 class="text align-self-center">Quên Mật Khẩu</h2>
                <p class="sub-text align-self-center">Nhập mật khẩu mới cho: <span style="color: red;">{{username}}</span> </p>
                <form method="post" class="form align-self-center">
                    <div class="form-group">
                        <img src="/lock.png" class="image">
                        <input type="password" id="password1" name="password1" class="input-field" placeholder=" ">
                        <label for="password" class="label">Mật khẩu mới</label>
                        <i class="far fa-eye" id="togglePassword1"></i>
                        <div class="checkPass1"></div>
                    </div>          
                    <div class="form-group">
                        <img src="/lock.png" class="image">
                        <input type="password" id="password2" name="password2" class="input-field" placeholder=" ">
                        <label for="password" class="label">Nhập lại mật khẩu</label>
                        <i class="far fa-eye" id="togglePassword2"></i>
                        <div class="checkPass2"></div>
                    </div>
                    <div class="d-flex flex-row justify-content-center">
                        <input type="submit" formaction="/changePass" value="Xác nhận" class="submit align-self-center mt-4">
                    </div>                        
                </form>

                {{#if error}}
                    <div class="alert alert-danger d-flex align-items-center mt-1 alert-dismissible" role="alert"> 
                        <i class="fas fa-exclamation-triangle danger_icon"></i> 
                        <div> {{message}} </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {{/if}}
            </div>
        </div>
    </div>
</body>
<!-- Jquery -->
<script src="https://code.jquery.com/jquery-3.6.0.js"></script>

<!-- Popper.JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.2.2/Chart.min.js"></script>

<script>
    $(document).ready(function(){
        $('#togglePassword1').click(function (e) {
            // toggle the type attribute
            const type = $('#password1').attr('type') === 'password' ? 'text' : 'password';
            $('#password1').attr('type', type);
            // toggle the eye slash icon
            $('#togglePassword1').toggleClass('fa-eye-slash');
        });

        $('#togglePassword2').click(function (e) {
            // toggle the type attribute
            const type = $('#password2').attr('type') === 'password' ? 'text' : 'password';
            $('#password2').attr('type', type);
            // toggle the eye slash icon
            $('#togglePassword2').toggleClass('fa-eye-slash');
        });

        function alert(message, type, element) {
            $(`.check${element}`).html('<div class="alert alert-' + type + ' d-flex align-items-center mt-1 alert-dismissible" role="alert"> <i class="fas fa-exclamation-triangle"></i> <div> ' + message + ' </div><button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button></div>');
        }

        // Kiêm tra ràng buộc ở đăng nhập
        var checkPass1 = 0, checkPass2 = 0;
        $('.submit').prop('disabled', true);
        $('#password1').keyup(function() {
            if($(this).val().length != 0)
            {
                checkPass1 = 1;
                $('.checkPass1').html('');
                if(checkPass1 == 1 && checkPass2 == 1)
                    $('.submit').prop('disabled', false);
            }
            else
            {
                checkPass1 = 0;
                alert('Vui lòng điền mật khẩu mới!', 'danger', 'Pass1');
                $('.submit').prop('disabled', true);
            }
        });
        $('#password2').keyup(function() {
            if($(this).val().length != 0)
            {
                checkPass2 = 1;
                $('.checkPass2').html('');
                if(checkPass1 == 1 && checkPass2 == 1)
                    $('.submit').prop('disabled', false);
            }
            else
            {
                checkPass2 = 0;
                alert('Vui lòng nhập lại mật khẩu mới!', 'danger', 'Pass2');
                $('.submit').prop('disabled', true);
            }
        });

        
    });

</script>
</html>